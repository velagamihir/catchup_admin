import 'package:flutter/cupertino.dart';

class AuthorityLogin extends StatelessWidget {
  const AuthorityLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Authority Login");
  }
}
