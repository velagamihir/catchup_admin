import 'package:flutter/cupertino.dart';

class SearchAuthorities extends StatelessWidget {
  const SearchAuthorities({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Search Authorities");
  }
}
